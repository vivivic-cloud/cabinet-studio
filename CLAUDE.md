# cabinet-studio — 집 규칙

파일 하나짜리 프로그램이다(`index.html`). `main` 에 올리면 깃허브 페이지로 바로 나간다:
https://vivivic-cloud.github.io/cabinet-studio/

## 0. 먼저 지킬 것

- **요청한 것 말고는 손대지 않는다.** 카드 모양·화면 짜임을 임의로 바꾸지 않는다.
- 위험해 보이면 **막지 말고 말한다.** 사장님이 시키신 기능을 내 판단으로 잠그지 않는다.
  (전에 붙박이 부속 삭제를 막았다가 되돌렸다. 숨기기를 3D 전용으로 바꿨다가도 되돌렸다.)
- 화면은 **폰 375px 기준**. 손에 닿는 것은 **최소 44px**. 1280px 도 같이 잰다.
- 답에는 **잰 숫자**를 적는다. 확인 못 한 것은 못 했다고 적는다.
- **관리자 판정을 조용히 뒤집지 않는다** (09-22 에 한 번 그랬다 — 썸네일 자리).
  판정이 틀려 보이면 **되돌리기 전에 까닭을 대고 말한다.** 급해서 먼저 했으면
  **커밋 글 첫 줄과 답 첫 줄에 「관리자 판정 ○번을 되돌렸습니다 — 까닭」** 이라고 적는다.
  관리자는 그 판정을 사장님께 이미 말씀드렸을 수 있다.
- **사장님 말씀이 곧 범위다.** 「아래에」 라고 하시면 아래다 — 넓은 화면에서 더 나아 보여도 내가 얹지 않는다.
  더 나은 길이 보이면 **하지 말고 숫자로 알린다.**
- **관리자 글이 서로 어긋나면 나중에 온 것이 이긴다.** 오가지 말고 나중 것 하나만 한다.
  그래도 헷갈리면 **고치기 전에 한 줄로 묻는다** — 두 번 커밋하는 것보다 싸다.
- **사장님이 글자 그대로 말씀하신 것과 관리자 판단이 부딪히면 사장님 글자가 이긴다.** 그때는 관리자에게 그렇게 말한다.
  (09-22 썸네일 자리 — 사장님 「아래에」 vs 넓은 화면에서 키우기. 사장님 글자가 이겼다.)

## 1. 덮기(cover) DXF — 뒷판 두께로 두 벌 잰다

**사장님이 뒷판 기본 두께를 9T → 2.7T 로 바꾸라 하셨다(09-16).** 그래서 기준 숫자가 두 개다.

| 기본값 | 덮기 DXF | 끼우기 DXF |
|---|---|---|
| 800×400×1800 · 고정선반 3 · 아웃도어 2 · 전면밴드 80 · **뒷판 2.7T** (지금 기본값) | **72,203** | **109,029** |
| 같은 값에 **뒷판 9T** (예전 기본값) | **71,124** | **108,135** |

**09-22 에 네 숫자가 98바이트씩 줄었다 — 결 방향이 기본 「결없음」 이 되었기 때문이다(§4.96).**
사장님이 시키신 것이라 맞는 변화다. 줄어든 98 = 부품표 결 칸 **7줄 × 14바이트**(한글 두 자)다.
**예전 값(측판·문짝·뒷판 세로, 나머지 가로)을 다 켜면 바이트까지 옛 숫자로 돌아온다 — 72,301 · 109,127 · 71,222 · 108,233.**
그래서 결을 안 건드리는 일이면 **기본값 네 숫자**로, 결을 건드린 일이면 **다 켠 네 숫자**로 견준다.

덮기 두 숫자는 09-16 **뒷판 2mm 들이기**로 바뀐 것이다(그 전에는 72,806 · 71,698).
**끼우기는 그때도 안 바뀌었다** — 덮기만 고치는 일이면 끼우기 숫자가 그대로인지로 확인한다.

**무엇을 고치든 이 네 숫자가 달라지면 잘못 건드린 것이다.**
뒷판 두께 말고는 아무것도 안 달라졌다는 것을 보이려면 **9T 로 돌려 71,124 가 나오는지** 재면 된다.
**이 글에 적힌 옛 숫자(71,698 · 72,806 · 72,301 …)는 그때그때의 기준이다 — 지금 기준은 위 표다.**

재는 법 — `buildDrawing` 은 DOM 안에서 도는 코드라 브라우저로 뽑는 것이 제일 확실하다.

```bash
# 1) 이 방은 CDN 이 막혀 있다. three.min.js 를 옆에 두고 주소만 바꾼 복사본을 만든다.
#    (index.html 의 cdnjs three 주소 → 'three.min.js', jszip <script> 는 지운다)
# 2) 그 폴더를 http-server 로 띄우고
# 3) 헤드리스 크로미움으로 열어 #btnDxf 를 눌러 받은 파일 크기를 잰다.
#    md5 로 예전 판과 줄줄이 견주면 더 확실하다 (다른 줄 0 이어야 한다).
```
견줄 때는 네 상태를 다 본다 — ① 기본 ② 인도어 ③ 전면밴드 0 ④ 값 여럿 바꿈.

## 2. `쪽` 값의 뜻 (buildDrawing 안)

도면 조각마다 「어디로 나가는가」 를 붙여 둔다.

| 쪽 | 뜻 |
|---|---|
| 0 | 화면·SVG·DXF 다 나간다 |
| 1 | **화면·SVG 만** — 꾸밈(도면 테두리, 투상 제목 밑줄, 새 표제란) |
| 2 | **DXF 만** — 예전 표제란. CAD 자료를 안 바꾸려고 남겨 둔 것이다 |

**꾸밈을 DXF 에 실지 않는다.** 종이 테두리 같은 것은 `OUTLINE` 층에도 넣지 않는다 —
`OUTLINE` 은 가구 외곽선 자리다(기본값에서 LINE 84개). 굵기만 다르게 하려면 `굵기:'강'` 을 쓴다(층은 그대로).

## 3. 뒷판 끼우기 치수 — 어디까지가 사장님 말씀인가

**사장님 말씀 (그대로 따른다)**
- 뒷판을 통 내부보다 가로·세로 **11mm** 크게 짠다 (`규칙.뒷판키움`)
- 홈 깊이 **6mm** (`규칙.홈깊이`)
- 홈 폭 = 뒷판 두께 + 유격, **2.7T 일 때 3.5** → 유격 **0.8mm** (`규칙.홈유격`)
- 홈을 파는 부속: 양 측판 · 상판 · 「맨바닥선반(제일아래선반)」

**아직 사장님 확인 전 (관리자가 정한 값 / 내가 읽은 것)**
- **`규칙.홈뒤살` 9mm** — 몸통 뒷면에서 홈까지. **관리자가 임의로 정했다.** 사장님 확인 전이다.
- **「맨바닥선반」 = 이 모델의 `하판`** 으로 읽고 있다. **사장님 확인 전이다.**
- 뒷판을 홈 **뒤쪽 벽에 붙여** 앉히고 유격은 앞쪽에 남긴다 — 내가 정했다.
- 이동 선반 여유(좌우 1mm · 앞뒤 10mm)도 관리자가 정한 값이다.

## 4. 선반은 뒷판 앞면에서 멈춘다 — 한 번 틀렸던 자리

```
선반끝 = 끼움 ? (홈뒤 − 뒷판두께) : 몸통깊이
선반깊이 = 선반끝 − 도어물림
```
끼우기에서 몸통 깊이만 늘리고 선반을 따라가게 두면 **선반이 뒷판을 뚫는다**(전에 9mm 뚫었다).
측판·상판·하판은 깊이를 다 쓰는 것이 맞다 — 그건 홈으로 물리는 자리다.
고친 뒤에는 설정을 여러 가지로 돌려 **선반↔뒷판 겹침 0** 을 확인한다(972가지 돌려 봤다).

## 4.5 도면 칸은 A4 여러 쪽이다

`#svgwrap` 안에 `.page` 를 세로로 쌓는다. 1쪽 = 여태 쓰던 전체도면, 2쪽부터 = 부속 도면(한 쪽에 3개까지).
부속 쪽은 `부속쪽SVG` 가 따로 짠다 — `viewBox="0 0 210 297"`, **단위가 곧 mm** 다.
**`buildDrawing` 의 `P` 를 건드리지 않는다.** 그래서 저장 SVG·DXF 는 이 쪽들을 모른다(덮기 DXF 그대로 · §1).
**09-22 부터 쪽은 한 장씩 보이고 나머지는 썸네일이다 — §4.97 이 지금 모습이다.**

- SVG 의 `text-anchor` **속성은 CSS 에 진다.** `.pg text{text-anchor:...}` 를 두면 속성이 다 무시된다 — 한 번 당했다.
- 글자는 화면과 종이에서 크기가 달라야 한다(폰에서 A4 한 장이 355px 로 눌린다). `.pg .t1~.t4` 를 미디어 쿼리로 나눠 뒀다.
- 숨긴 부속은 부속 쪽에서 뺀다(§6). 부품표에는 남는다.
- **부속 차례** 는 `부속차례` 한 줄이 정한다 — 몸통 → 문짝 → 선반 → 전면밴드 → 뒷판, 만든 부속은 맨 뒤.
  자주 보는 것이 앞이다. **부품표(`bomRows`)의 차례는 이것과 별개다 — 같이 바꾸지 마라.**
- 1쪽 아래 **차례**(`.toc`)와 쪽마다 **머리글에 그 쪽 부속 이름**이 있다.
  「문짝이 없다」 는 말을 들은 적이 있다 — 맨 뒷쪽에 있는데 못 찾으신 것이었다. **있는데 안 보이면 없는 것이다.**

- **경고 깜박임은 이 방에서 잴 수 있다.** 조각의 `material.color.getHexString()` 을 0.1초마다 찍으면
  `d9b98a` ↔ `ff3b30` 이 번갈아 나온다. 「3D 라서 못 봤다」 고 적지 마라(§7).
- **홈 치수는 지시선으로 빼지 않는다.** 좁아서 숫자가 안 들어가면 **상세도를 키운다** — 캐드에서 좁은 자리를 재는 법이다.
  그래서 홈 파는 부속은 **한 쪽을 통째로 쓴다**(`무게 = A4.칸수`). 배율은 칸 너비가 정한다(기본값에서 4.7배).
  한 번 지시선으로 빼냈다가 「도면처럼 하라」 고 되돌린 자리다.
- **홈 가공도는 그 부속 쪽에 있다.** 1쪽(메인)에서는 `쪽 = 2` 로 돌려 화면·저장 SVG 에서 뺐다 —
  **DXF 에는 그대로 있다.** 거기서까지 지우면 끼우기 DXF 108,233 바이트가 달라진다.
  홈 치수(폭·깊이·뒤 끝에서)는 홈이 3.5mm 라 치수선 안에 숫자가 안 들어간다. **지시선으로 빼서 적는다.**

## 4.55 치수 숫자는 화면에서만 자리를 다시 잡는다 — **재서 고르는 것이지 계산하는 것이 아니다**

`글자배` 로 치수 숫자를 최대 3배로 키우는데 자리는 1배 기준(치수선에서 23)이라 숫자가 제 치수선을 덮었다.
**한 번 셈으로 밀었다가 크게 틀렸다** — 물체 쪽으로 밀어 **부속 네모 위에 올라탔다(360건)**. 사장님이 보셨다.
지금은 `renderSVG` 가 **후보 자리 일곱 군데를 차례로 재서 아무 데도 안 닿는 데를 고른다.**

```
① 예전 자리(안 겹치면 안 옮긴다) → ② 제 치수선 바로 안쪽 → ③ 제 치수선 바깥
→ ④ 물체에 바짝 → ⑤⑥ 치수선 끝 바깥(양쪽) → ⑦ 한 단 더 바깥 → 다 막히면 지시선
```
- 막이는 **부속 네모(`d.hits`) · 화면에 그려지는 모든 선(여유 5) · 다른 글자** 셋이다. **부속 네모를 빼면 안 된다.**
- 글자는 놓는 대로 `글자막이` 를 갱신한다 — 뒤에 오는 숫자가 그 자리를 피한다.
- 다 막히면 **지시선**(`<line class="DIM 지시">`)을 그어 바깥으로 뺀다. 108가지에서 48개 나온다.
  이 선은 `renderSVG` 가 직접 뱉고 **`P` 에는 안 넣는다** — 그래서 DXF·저장 SVG 가 안 변한다.
- **`재배치` 는 `화면 && 글자배 > 1` 일 때만 참이다.** 이 조건을 풀지 마라 — 내보내는 파일이 달라진다.
- 글상자는 **재서 정한 값**이다: 숫자 한 자 폭 `0.601×fs`, 글자 높이 `0.72×fs`. 코드는 여유를 얹어 `0.62`·`0.80` 을 쓴다.
- 선 여유는 **5** 다. 8 이나 12 로 키우면 8단 선반의 「71.1」 들이 서로 닿는다(24건). 5 가 제일 낫다.

**재는 법 — 여기서 두 번 헛짚었다.**
- **부속은 `<rect>` 가 아니라 선 넷이다.** `rect` 로 찾으면 0건이 나온다.
  보이지 않는 짚을 자리 **`rect.pick` 이 곧 부속 네모**다. 그걸로 재라.
- **`getBoundingClientRect` 는 선 굵기를 끼워 넣는다.** `.pick` 은 굵기가 **22px** 이라 네모가 사방 11px 부푼다.
  도형은 **`getBBox()` + `getScreenCTM()`** 으로 재라(굵기 없음). 선만 굵기 반을 따로 얹는다.
- **90도 세운 글자는 em 상자가 가로로 눕는다.** 글자 네모의 여백을 깎을 때 축을 바꿔야 한다.
  세로 축만 깎으면 세로 치수에서 없는 겹침이 12건 잡힌다.
- 갈래는 넷이다 — **제 치수선 · 다른 치수선 · 부속 네모 · 다른 숫자**. 갈래마다 건수를 적어라.
  설정은 폭 600·800·1200 × 높이 900·1800·2400 × 선반 0·3·8 × 인·아웃도어 × 덮기·끼우기 = 108가지.

**잰 값 (09-17 · 치수 숫자 1,692개)**

| | 제치수선 | 다른치수선 | 부속네모 | 다른숫자 |
|---|---|---|---|---|
| 전 · 375px (1px 넘게) | 0 | 12 | **288** | 0 |
| 전 · 1280px | 0 | 12 | **330** | 36 |
| 후 · 375px · 1280px | **0** | **0** | **0** | **0** |
| 전 · 375px (스치기만 해도) | 144 | 156 | 396 | 108 |
| 후 · 375px (스치기만 해도) | **0** | **0** | **0** | **0** |
| 후 · 1280px (스치기만 해도) | 12 (제 끝자국, **0.46×0.15px**) | 0 | 0 | 0 |

**치수선 간격(off)을 화면에서 벌려 보려다 되돌렸다** — 치수선이 내려가며 숫자가 「정면도 FRONT VIEW」 를 덮었다.
`화면도면 = drawing` 그대로 두어라. `buildDrawing(s, m, 치수배)` 의 셋째 인자는 남겨 뒀지만 아무도 1 말고는 안 준다.

## 4.58 2D · 3D 모드 — 한 번에 하나만 보인다 (09-22 사장님 말씀)

사장님 말씀: 「제작도면 쪽은 2D보기 변환하면 현 3d 화면 사라지고 보이게 해줘 2d 3d 모드변환 으로 둘중 하나만 보기로 하자」

- **가르는 것은 `.view3d` 와 `.draw` 딱 둘이다.** `.app` 에 `m3d`/`m2d` 를 걸어 `display:none` 으로 **확실히** 숨긴다.
- **부품표(`.bom`)는 두 모드 다 그대로 보인다.** 사장님이 숨기라 하신 적 없다 — 같이 숨기지 마라.
- **처음 열면 3D** (`<div class="app m3d">`).
- 바꾸는 단추(`.modeseg`)는 **두 칸에 하나씩** 있다 — 3D 칸의 `.hud` 맨 앞, 도면 칸의 `.panelhead` 끝.
  한 번에 한 칸만 보이므로 늘 눈앞에 있다. **52×44px**, 켜진 쪽이 검게 찬다.
  **`.hud` 의 조용히 숨기기 목록(§4.5)에 넣지 마라** — 이것만은 늘 보여야 모드를 되돌릴 수 있다.

**모드마다 칸을 다시 나눈다** — 숨은 칸 자리를 남은 칸이 실제로 가져간다.
```
≥1101px  m3d: "head head" / "params view3d" / "params bom"
         m2d: "head head" / "params draw"   / "params bom"
≤1100px  한 줄씩 쌓되 숨긴 칸을 아예 뺀다
```

**다시 켤 때 반드시 다시 재야 한다 — 여기서 까맣게 남는다.**
- 3D: 숨은 동안 `wrap.clientWidth/Height` 가 **0** 이라 `camera.aspect` 가 **NaN** 이 된다.
  `resize()` 앞에 **`if (!w || !h) return;`** 를 두고, `삼디크기맞추기()` 를 밖으로 빼 모드를 켤 때 다시 부른다.
- 2D: 숨은 동안 `#svgwrap.clientWidth` 가 0 이라 `글자배()` 가 틀어진다. 켤 때 **`도면그리기()`** 를 다시 부른다.
- 둘 다 `requestAnimationFrame` 안에서 부른다 — 칸이 자리를 잡은 뒤라야 제대로 잰다.

**잰 값 (진짜 손가락·마우스로 눌러 2D↔3D 를 세 번씩 오갔다. 오류 0)**

| | 고치기 전 | 3D 모드 | 2D 모드 |
|---|---|---|---|
| 1280 3D 칸 | 532×469 | **995×469** | 숨김 |
| 1280 도면 칸 | 462×833 | 숨김 | **995×469** |
| 1280 `.svgwrap` | 462×790 | – | **995×404** |
| 1280 **A4 한 쪽** | **442×626** | – | **975×1379** (2.2배) |
| 1280 부품표 | 532×363 | 995×363 | **995×363** (가로굴림 없음) |
| 375 문서 높이 | 2,913 | **2,444** | 2,444 |
| 375 A4 한 쪽 | 355×502 | – | 355×502 (그대로 — 폰은 원래 한 칸) |

- **A4 쪽 크기 이야기는 09-22 에 §4.97 로 바뀌었다** — 이제 한 쪽만 칸에 맞춰 통째로 보이고 나머지는 썸네일이다.
  (여기 적혀 있던 「1280 에서 쪽의 29% 만 보인다」 는 그것으로 풀렸다.)
- 3D 칸의 `.hud` 는 375px 에서 83 → **145px**(세 줄)이 된다. `.hud` 는 `position:absolute` 라 **캔버스는 안 줄어든다**(375×468 그대로).
- 캔버스·카메라 — 375 `750×936 · aspect 0.801`, 1280 `995×469 · aspect 2.122`. 세 번 오가도 **NaN 0 · 삼각형 132 그대로 · 까맣게 안 남는다**.
- **길게 누르기(짚어서 지시)는 두 모드에서 다 된다** — 3D·2D 각각에서 지시창이 뜨고 「모드 시험」 이 그대로 쳐진다.

**시험 짤 때 조심** — 도면을 재는 자는 **2D 모드로 바꾼 뒤에** 재야 한다.
3D 모드에서는 `#svgwrap` 이 `display:none` 이라 **치수 숫자가 0개로 나온다**(한 번 그렇게 0 을 보고 놀랐다).

**위 표의 2D 칸 숫자는 09-22 §4.97 로 또 바뀌었다** — 2D 모드에서 도면 칸에 세로를 더 주기 때문이다.
지금은 1280 도면 칸 **598** · `.svgwrap` **995×533** · 부품표 칸 **234**, 폰 도면 칸 **655** 다. **§4.97 이 지금 모습이다.**

## 4.59 3D 돌리기 — 짚은 점을 축으로, 절반쯤 느리게 (09-22 사장님 말씀)

사장님 말씀: 「**커서가 멈춰진 부분 기준으로 뷰가 돌아가야** 하는데 너무 휫휫 **어라운드 범위가 커서**」.

**빠르기** — `궤도속 = 0.00419` 한 값이다(전에는 `0.006` 이 두 곳에 박혀 있었다).
폰 폭 375px 을 한 번 쓸면 90도다: `(Math.PI/2) / 375`. **줌 드래그의 `0.006` 은 건드리지 않았다.**

**축** — 끌기 시작할 때 **한 번만** 겨눠(`축잡기`) 그 점을 잡고, 도는 동안 그 점이
**화면에서 한 픽셀도 안 움직이게** 기준(`tTgt`)을 거꾸로 셈한다.

```
축 = { 점: 세상 자리, 속: 그때 카메라 자리에서 본 같은 점 }      // 끌기 시작 때 한 번
새 카메라 = 축.점 − (자세(새θ,새φ) × 축.속)
tTgt      = 새 카메라 − 시선방향 × tR
```
- `자세(θ,φ)` 는 three 의 `lookAt` 과 같은 셈이다(z = 기준→카메라, 위는 +Y). **이 식을 바꾸지 마라** —
  `tTgt` 만 옮기고 `r` 을 안 맞추면 **짚은 점이 화면 가운데로 순간이동한다.**
- **겨눔은 `aimCam`(목표 시점)으로 한다** — 화면 카메라는 목표를 뒤쫓는 중이라 기준이 밀린다. 줌과 같은 길이다.
- **빈 곳을 짚으면 축이 없다** → 예전대로 지금 기준으로 돈다. `mode !== 'orbit'` 이면 `축 = null` 이다.
- `r` 은 도는 동안 안 바뀐다. 그래서 짚은 점은 **화면 자리와 깊이가 둘 다** 그대로다.

**잰 값 (진짜 손가락·휠 드래그. 8자리 × 네 방향)**

| | 고치기 전 | 고친 뒤 |
|---|---|---|
| 375 폰 · 캔버스 폭 363px 한 번 쓸기 | **124.8°** | **87.1°** (375px 이면 90.0°) |
| 1280 · 캔버스 폭 983px 한 번 끌기 | **337.9°** | **236.0°** |
| px 당 | 0.3438° | **0.2401°** |
| 짚은 점이 화면에서 밀린 거리 (폰) | 0 · 29.2 · 30.2 · **48.4px** | **다 0.00px** |
| 짚은 점이 화면에서 밀린 거리 (1280) | 17.5 · 28.6 · 37.4 · **46.6px** | **다 0.00px** |
| 끌기 시작 순간 튐 | 폰 5.22px · 1280 0.27px | **둘 다 0.00px** |

**마우스와 손가락은 같은 값을 쓴다.** px 당 각도가 같으니 같은 거리를 끌면 같이 돈다 —
넓은 화면은 캔버스가 넓어(995px) 한쪽에서 한쪽까지 끌면 236° 돈다. 캔버스 폭에 비례하게 바꾸지는 **않았다**(관리자가 정한 것).

**안 뺏은 것 (전·후가 한 글자도 안 다르다)** — 이동(`pan`) · 줌(휠) · 전체(Shift+Z, r 3920 · 기준 0,0,0) ·
시점 단추 다섯(등각 0.75/1.15 · 정면 0/1.5708 · 측면 1.5708/1.5708 · 위 0/0.02 · 뒤 3.1416/1.5708) ·
**길게 누르기(지시 손잡이)** — 폰에서 0.9초 누르니 `.vg-sheet` 가 뜨고 「궤도 시험」 이 들어가고 **카메라는 안 움직였다** ·
**밴드 3D 끌기** — 뒷판을 숨기고 밴드를 골라 끌어 z **292.554** 로 전·후가 같음.
네 기준값 그대로(그때는 72,301 · 109,127 · 71,222 · 108,233 · §1 참고), 저장 SVG 네 상태 다 같음. 오류 0.

## 4.97 2D 도면은 한 쪽씩 보고, 나머지는 썸네일로 고른다 (09-22 사장님 말씀)

사장님 말씀: 「**2d시 화면이 너무 커서 전체를 둘러보기 어렵습니다. 현재페이지를 적당한 사이즈로 줄이고
나머지 부분은 아래에 썸네일로 페이지 선택해서 볼 수 있게** 해주세요」

`#svgwrap` 이 **`.pagebox` + `.thumbs`** 두 칸으로 나뉜다.
쪽은 예전처럼 **다 만들어 두고** `#pageBox` 안에 넣되, 고른 한 쪽만 보인다.

```
#pageBox > .page:not(.보는쪽){display:none}      /* 아이디로 쓴다 — `.svgwrap .page.p1{display:flex}` 가 뒤에 와서 이긴다 */
@supports (container-type:size){ .pagebox{container-type:size}
  .pagebox>.page{width:min(100cqw, calc(100cqh * 210 / 297))} }
```
- **쪽 고르기(`쪽고르기`)는 다시 그리지 않는다** — `보는쪽` 클래스만 옮긴다. 썸네일도 그대로 둔다.
- 썸네일은 **쪽 그림을 그대로 베낀다**(`svg.cloneNode(true)`). 다시 그리지 않으므로 값이 바뀌어도 같이 따라온다.
  베낀 그림 안의 `.pick` 은 `pointer-events:none` 이라 누르면 **썸네일 단추**가 눌린다.
- **`보는쪽` 은 값을 바꿔 다시 그려도 그대로다.** 쪽 수가 줄면 마지막 쪽으로 물린다(잰 값: 4쪽 보다가 3쪽이 되면 「3 / 3」).
- `container-type:size` 를 못 받는 브라우저는 예전처럼 **폭에 맞춘다**(`width:100%`). 그래도 탈은 안 난다.
- **인쇄는 그대로 네 쪽이 다 나간다** — `@media print` 에서 `#pageBox>.page{display:block;width:210mm}`,
  `.thumbs{display:none}`, `container-type:normal`. 잰 값: 네 쪽 다 **794×1123**.
- `글자배()` 는 이제 **`#pageBox` 크기**로 잰다 — 세로가 모자라면 세로가 폭을 정하기 때문이다.
  `ResizeObserver` 도 **높이까지** 본다(예전에는 너비만 봤다).
- `큰그림열기` 는 `$('#pageBox').children[쪽번호]` 를 베낀다(예전엔 `#svgwrap`). 「크게」 는 그대로다(§4.75).

**썸네일은 어느 폭에서나 「아래에 한 줄」 이고 가로로 민다. 옆에 세우지 마라.**
사장님 말씀이 「**아래에**」 다. 넓은 화면에서 옆에 세우면 쪽이 288×407 → 363×513 으로 커지지만
**사장님이 청하지 않은 것이다**(사장님 서 있는 규칙: 「요청내용외 절대 수정금지」). 사장님은 폰을 쓰신다.
**09-22 에 세 번 오갔다 — 옆 → 아래 → 옆 → 아래.** 마지막이 맞는 것이다. 더 흔들지 마라.

**쪽이 한 장뿐이면 썸네일 줄을 아예 안 만든다.** `.thumbs:empty` 로는 안 된다 —
`#thumbs` 에는 1쪽 단추가 **늘 하나는** 들어가서 `:empty` 가 영영 참이 안 된다.
`#thumbs.hidden = 총 < 2` 로 감추고 베끼기도 `if (총 > 1)` 로 막는다.
잰 값(붙박이를 다 지워 1쪽만 남겨 봤다) — 썸네일 줄 **0×0 · 단추 0**, 그만큼 칸이 넓어져 쪽이 커진다:
**375 쪽칸 465 → 570 · 쪽 329×465 → 355×502**, **1280 쪽칸 407 → 513 · 쪽 288×407 → 363×513**.
되살려 네 쪽이 되면 썸네일 줄이 **도로 나온다**(355×105 · 975×105).

**2D 모드에서는 도면 칸에 세로를 더 준다.** 2D 는 도면을 보러 들어간 자리다.
- 폰: `minmax(380px,84vh)` (3D 모드는 60vh 그대로)
- ≥1101px: `grid-template-rows:auto minmax(440px,1fr) minmax(120px,26vh)` — **부품표 칸만 364 → 234 로 줄었다.**
  부품표는 제 안에서 굴러가므로 다 볼 수 있다. **3D 모드는 한 줄도 안 바뀌었다.**

**잰 값**

| | 고치기 전 | 고친 뒤 |
|---|---|---|
| 375 A4 한 쪽 | 355×502 (굴려야 다 본다) | **329×465 · 통째로 보임**(세로·가로 굴림 0) |
| 375 **1쪽 다 보려 굴릴 거리** | **99px** | **0px** |
| 375 도면 칸 | 468 | **655** (2D 모드일 때만) |
| 375 썸네일 줄 | – | 355×105 · 단추 **56×94** 네 개, 한 화면에 4장 |
| 1280 A4 한 쪽 | 975×1379 (굴릴 거리 **975px**) | **288×407 · 굴릴 거리 0** |
| 1280 썸네일 줄 | – | 975×105 (아래) |
| 1280 도면 칸 · 부품표 칸 | 469 · 363 | **598 · 234** (2D 모드일 때만 · 부품표 가로굴림 없음) |

- 도면 위를 톡 쳐서 부속 고르기 **그대로 된다**(잰 값: 톡 → 「고정선반」).
- **인쇄는 전·후 똑같이 네 쪽**이고 썸네일 줄은 높이 0 이다.
- **「크게」 는 쪽마다 제 쪽을 연다** — 네 쪽을 하나씩 골라 눌러 「1쪽 → 1 / 4」 … 「4쪽 → 4 / 4」 로 다 맞았다.
- **쪽이 열 장 넘어도 가로로 민다** — 만든 부속 30개로 14쪽을 만드니 썸네일 14개, 속폭 892 / 칸폭 355,
  세로로 안 쌓이고, 끝까지 밀어 마지막을 고르니 「14 / 14」.
- 2D↔3D 세 번 오가도 쪽·크기가 그대로고(3 / 4 · 363×513) 3D 삼각형 132. **오류 0.**
- 내보내는 것은 **한 점도 안 바뀐다** — DXF 네 값 그대로, 저장 SVG 네 상태 **조각까지 같음**(975·966·957·1187).
- 108가지 치수 겹침 쓸기 다시 돌림(글자배가 달라지므로 꼭 다시 돌려야 한다) — 숫자 1,692 · 지시선 48 · **겹침 0**.

## 4.6 화면 틀 — 넓은 화면은 한 장에 못 박혀 있다

`@media (min-width:1101px)` 에서 `.app{height:100vh}` 다. 칸마다 제 안에서 굴린다(`.svgwrap`·`.params`·`.tablewrap`).
**이걸 풀면 3D 칸이 좌우 칸 길이를 따라 늘어난다** — 전에 2,237px 까지 늘어나 사장님이 말씀하셨다.
`.svgwrap{overscroll-behavior:contain}` — 도면 끝까지 굴려도 문서가 안 따라간다.
1100px 이하(폰)는 위아래로 쌓이는 그대로다. 손대지 마라.

## 4.7 폰에서 3D 에 집중하기

폰(1100px 이하)에서 **3D 를 만지고 손을 뗀 뒤** 설정 칸이 접힌다(`.params.접힘` · `.app.집중`).
- **만지는 중에는 안 접는다.** 손가락 밑에서 화면이 튀면 안 된다.
- 한 번 접히면 그대로 둔다. 다시 펴는 길은 **접힌 자리의 「설정 펴기」 단추**(44px)뿐이다. 없애지 마라.
- 움직임은 `requestAnimationFrame` 으로 준다(220ms). 아이폰에서 CSS 움직임이 멎는 일이 있다.
- `pointerup` 만 듣고 아무것도 막지 않는다 — **길게 누르기(지시 손잡이)를 가로채면 안 된다.**
- 넓은 화면은 안 접는다. 3D 칸: 폰 468px → 접히면 **655px**.

**손가락 두 개로 벌려 키우기(핀치)는 원래 없다.** `wheel` 만 있다 — 폰에서는 「줌」 연장을 고르고 끈다.

**도구줄(선택·궤도·이동·줌·전체)은 이제 폰에만 있다** (09-17 사장님 말씀 「마우스로 다 된다」).
`@media (hover:hover) and (pointer:fine)` 에서 `.hud .tools{display:none}` 다. 자바스크립트는 한 줄도 안 바꿨다.
**마우스는 원래 이 단추를 안 본다** — `pointerdown` 에서 `tool` 은 `e.pointerType === 'touch'` 일 때만 읽는다
(마우스: 왼쪽 끌기=이동 · 휠 끌기=궤도 · Shift+휠 끌기=이동 · 스크롤=줌 · Shift+Z=전체).
**손가락에는 이 단추가 유일한 길이다.** 핀치가 없으니 폰에서 지우면 돌리기·옮기기·줌이 통째로 사라진다 — 지우지 마라.
힌트 줄의 「· Shift+Z 전체」 도 `.mouseonly` 로 마우스에서만 나온다(폰 힌트는 272×16px 그대로).

## 4.75 도면 한 쪽을 화면 가득 보기 (09-17 사장님 말씀)

쪽마다 **「크게」 단추**(56×44px, 오른쪽 위)를 두었다. 누르면 `#zoomDlg` 가 그 쪽을 **베껴 와** 화면 가득 편다.
**도면 위를 그냥 톡 치는 것은 예전 그대로 부속 고르기다** — 그걸 뺏으면 안 되어 단추를 따로 둔 것이다. 바꾸지 마라.

- 손가락 하나 = 밀기 · 둘 = 벌려 키우기(**여기는 핀치가 있다**) · 휠 = 줌 · `−`·`＋`·`맞춤`·`닫기` 단추(44px 이상).
- **베껴 오므로 `#svgwrap` 은 한 점도 안 바뀐다.** 닫으면 쪽 수·글자 수·스크롤이 그대로다(잰 값: 4쪽·193자·y 1758 전후 같음).
- `dialog.zoom[open]{display:flex;flex-direction:column}` 이다. **`.zoomwrap` 높이를 px 로 박지 마라** —
  `top:61px` 로 박았더니 375px 에서 머리줄이 두 줄로 접히며 **그림이 단추 밑으로 파고들었다.**
- `.zoomwrap` 은 `svgwrap` 클래스를 같이 달아 도면 CSS 를 그대로 쓴다. 단 `.pick` 은 `pointer-events:none` 이다
  (안 그러면 부속 짚기가 미는 손을 가로챈다). `.big` 은 큰 그림 안에서 숨긴다.

## 4.8 밴드 — 사장님이 자리를 일러 주신 만든 부속 (09-16 고쳐 주신 규칙)

「부속 만들기」 로 **「밴드」** 라는 이름을 넣으면 `buildModel` 이 자리를 잡아 세운다(`자리있는부속`).

**재단은 두 방식 모두 같다.** 가로 = 내부 폭(W − 2·측판), 세로 = 전면밴드 + 하판 두께.
기본값에서 **764 × 98**. 사장님이 확인해 주신 숫자다. **이 식을 새로 만들지 마라.**

**하판 위로 올라탄다.** z 시작 = `bz + Tbot`(기본값 98). 바닥이 아니다.
**하판은 깊이를 다 쓴다** — 전에 밴드 두께만큼 줄였다가 사장님이 「올라탄다」 고 고쳐 주셔서 되돌렸다.

| 뒷판 | 밴드 앞뒤(y) | 기본값 | 튀어나옴 |
|---|---|---|---|
| 덮기 | `CD − 두께` ~ `CD` (가구 안쪽, 뒷판과 맞닿음) | 379.3 ~ 397.3 | 언제나 **0** |
| 끼우기 | `홈뒤` ~ `홈뒤 + 두께` (뒷판 바깥에 올라탐) | 391 ~ 409 (18T) | **두께 − 홈뒤살** |

**상 · 중 · 하 — 어느 단에 세울지 (09-17 사장님 말씀)**
밴드 줄 가운데에 **「상 · 중 · 하」 체크 단추**가 있다(56×31px, 닿는 자리 44px). 여러 개 켜지고, 다시 누르면 꺼지고, 셋 다 끌 수도 있다.
값은 `c.단` 에 담는다. **예전에 만든 밴드에는 이 값이 없다 — 그때는 「하」 하나뿐이었으므로 `밴드단()` 이 `['하']` 로 읽는다. 이 기본값을 바꾸지 마라.**

| 단 | 어디에 | 언제 | 기본값 z |
|---|---|---|---|
| 하 | 여태 있던 그 자리 — 하판 위로 올라탄다 | 언제나 | 98 ~ 196 |
| 중 | **고정선반**마다 하나씩, 그 선반 **센터 높이**에 맞춘다(`고정센터`) | **뒷판 끼우기일 때만** | 466·891·1317 (각 ±49) |
| 상 | 상판 바로 아래 하나 — 위끝이 `H − Ttop` | 언제나 | 1684 ~ 1782 |

**「중」 은 뒷판 끼우기일 때만 선다 (09-17 사장님 말씀).** `if (단.includes('중') && 끼움)` 한 줄이다.
덮기에서는 밴드가 가구 **안쪽**(`CD − 두께`)에 앉아 선반 깊이와 그대로 겹쳤다(764×18×18).
끼우기는 밴드가 **뒷판 바깥**(`홈뒤`)이라 선반이 거기까지 안 나온다 — **선반 뒤끝 388.3, 밴드 앞면 391.0, 틈 2.7mm(= 뒷판 두께).**
설정을 여러 가지로 돌려 **끼우기에서 중 밴드 ↔ 고정선반 겹침 0** 을 확인했다(기본·선반8·H2400·인도어·전면밴드0·W1200 다 0).

- 덮기에서 「중」 단추는 **흐리게(opacity .38) 두되 죽은 단추가 아니다** — 눌리면 `toast('「중」 은 뒷판 끼우기일 때만 섭니다')`
  가 뜨고 체크가 되돌아간다. `c.단` 에는 안 들어간다. 관리자가 정한 것이다.
  뒷판 방식을 바꾸면 `부속그리기()` 를 다시 불러 흐림이 따라간다 — **이 호출을 지우지 마라.**
- **이동선반은 세지 않는다** — 사장님 말씀이 「고정선반 수량에 맞게」 다. 이동만 8단이면 중 밴드는 0개다.
- 재단·앞뒤(y)·넘침 규칙은 세 단이 다 같다. 15mm 경고도 그대로 걸린다(끼우기 25T 에서 붉게 8번, 15 로 자동조정, 카메라 되돌아옴 — 밴드 5개에서 재 봤다).
- **상·중 밴드의 높이는 사장님이 안 정해 주셨다.** 하와 같은 `bz + Tbot` 로 두었다 — **내가 정한 값이다.** 그대로 두라 하셨다.

**아직 남아 있는 겹침 (사장님 확인 전 · 안 밀었다)**

| 어디 | 무엇이 | 얼마 |
|---|---|---|
| 덮기·끼우기 | **하 ↔ 맨 아래 선반** (예전부터 있던 것, 아래 표 참고) | 2.5 ~ 15.9mm |
| 덮기·끼우기 | **상 ↔ 맨 위 선반** — 하의 거울이다. 키 낮고 칸 많은 장에서 난다 | 764×18×16~18 |
| 끼우기만 | **밴드끼리** — 칸 높이가 98 보다 좁으면 중끼리·중↔하·중↔상 이 겹친다 | 9 ~ 67mm |

겹치지 않는 최소 높이(전면밴드80·하판18·고정선반만·상중하 다 켬):
**끼우기 3단 730 · 8단 1510 / 덮기 3단 570 · 8단 1150.** H2400 에서는 어느 쪽도 0 이다.

**경고 — 밴드가 측판 뒤끝을 넘을 때** (`밴드살피기` · `밴드깜박`)
- **두께가 15 를 넘고**(15 는 그냥 둔다 — 자동조정 값도 15라 15에서 경고하면 끝없이 돈다)
  **`m.밴드넘침 > 0` 일 때만** 뜬다. 덮기는 넘침이 0이라 아무리 두꺼워도 안 뜬다(거짓말이 되니까).
- 3D 가 그 자리로 다가가 등각으로 돌고 밴드가 붉게 8번 깜박인 뒤,
  두께를 **15 로 자동 조정**하고 `#warnDlg` 로 알린다. 지나갈 길은 없다.
- **끝나면 카메라를 보시던 자리로 돌려놓는다**(`본자리`·`되돌리기`). 한 번 안 돌려놔서
  가구 뒤 밴드에 붙은 채 서 있었고, 앞면 문짝이 화면 밖으로 나가 사장님이 **「3D 도면 도어 사라짐」** 이라 하셨다.
  문짝은 멀쩡했다 — 카메라만 거기 있었다. **이 되돌리기를 지우지 마라.**
- 깜박이는 중에 모델이 새로 서면(`!조각.parent`) 옛 조각을 놓고 마무리한다. 타이머는 `깜박타이머` 다.

**알아 둘 것 (관리자에게 말해 뒀다)**
- 만든 부속 두께 고르개에는 **9T 가 없다**(`두께값 = [12,15,18,23,24,25,28,55]`).
- **12T 는 끼우기에서 3mm 튀어나오는데 경고가 안 뜬다.** 경고 기준이 「두께」 라서 그렇다.
- **밴드가 맨 아래 선반과 겹치는 자리가 있다.** 밴드가 하판 위로 98 서기 때문이다.
  **기본값(H1800 · 전면밴드80 · 하판18 · 덮기)에서도 슬라이더로 갈 수 있다** — 고정과 이동을 **합쳐서** 세어야 한다:

  | 고정+이동 | 맨 아래 선반 z | 밴드 z | 겹침 |
  |---|---|---|---|
  | 합계 14 (6+8 · 7+7 · 8+6) | 193.5 | 98 ~ 196 | **2.5mm** |
  | 합계 15 (7+8 · 8+7) | 186.4 | 98 ~ 196 | **9.6mm** |
  | 합계 16 (8+8) | 180.1 | 98 ~ 196 | **15.9mm** |

  **밴드 두께와 상관없다**(12T~55T 다 같다 — 위아래로 겹치는 것이라서). 겹치는 조합은 6가지다.
  **끼우기는 안 겹친다** — 선반이 388.3 에서 끝나고 밴드는 391 부터라 앞뒤가 안 만난다.
  키 낮은 장은 더 쉽게 겹친다 — 전면밴드80·하판18 기준 겹치지 않는 최소 높이 고정 3단 **562**, 8단 **1142**, 8+8 **2070**.
  전면밴드 **120 · 8+8** 은 2400 까지 올려도 계속 겹친다. 사장님 확인 전이라 안 고쳤다.
  ※ **한 갈래만 돌려 「각각 14단이라 못 간다」 고 잘못 적은 적이 있다.** 선반은 두 갈래를 **합쳐서** 재라.
- **`밴드넘침` 은 지움을 거른 뒤 0 으로 돌린다.** 안 그러면 **지운 밴드에 「튀어나옵니다」 창이 뜨고
  두께가 제멋대로 15 로 바뀐다.** 숨긴 밴드는 산것에 남으므로 경고가 그대로 떠야 한다 — 그게 맞다.
- **`손질` 열쇠(`자리`)에 y 가 들어간다.** 밴드는 덮기·끼우기에서 y 가 달라지므로
  **덮기에서 지운 밴드가 끼우기로 바꾸면 되살아난다**(열쇠가 안 맞는다). 붙박이 부속도 같은 성질이다.
  사장님께 아직 안 여쭈었다.
- 이름 하나(`자리있는부속`)에 규칙이 묶여 있다. 다른 이름으로 지으시면 안 먹는다.

## 4.895 숨기기 취소는 3D 칸 오른쪽 위 — 44px 이다 (09-22 사장님 말씀)

사장님 말씀: 「**숨기기 취소 어디갔니?** 도어는 또 어디가고?」 (시점 단추 줄을 짚으셨다. 도어 쪽은 §4.86 이다.)

**있는데 안 보였다 — 28px 이었다.** 재 보니 가려지지도, 화면 밖으로 나가지도 않았다.
`.hidbar button` 을 **`min-height:44px`** 로 올렸다(글자 12→13px, 옅은 그림자). CSS 한 줄이다.

| | 전 | 후 |
|---|---|---|
| 「n개 숨김」 | 75×**28** | **83×44** |
| 「모두 보이기」 | 90×**28** | **99×44** |
| 자리 · 375px | 캔버스 위에서 167px · 오른끝에서 14px | 그대로 |
| 자리 · 1280px | 캔버스 위에서 74px · 오른끝에서 14px | 그대로 |
| 맨 위에 있나(`elementFromPoint`) | **제것** (안 가려짐) | 제것 |
| 4.2초 뒤 (도구줄·시점줄이 흐림 0 으로 사라진 뒤) | **흐림 1** (안 사라진다) | 흐림 1 |

- **`숨김바` 는 숨긴 것이 하나도 없으면 아예 안 뜬다**(`hidden`). 그게 맞다 —
  다만 **× 로 「지운」 것은 숨김이 아니라 여기 안 나온다.** 지운 것 되돌리기는 「부속 만들기」 칸의 드롭다운이다(§4.89).
  사장님이 「숨기기 취소 어디갔니?」 하신 것은 **문짝을 × 로 지우신 뒤**일 수 있다. **관리자에게 물어 뒀다 — 바에 「n개 지움」 을 붙이지 않았다.**
- 목록(`#hidmenu`)은 270×133 @화면 안, 줄 **256×44**. 한 줄 눌러 하나만 되돌리기 · 「모두 보이기」 둘 다 잰 대로 된다.
- **`.hud` 의 조용히 사라지는 목록(`['.hud .tools','.hud .views','.hint','.explode']`)에 `hidbar` 를 넣지 마라.** 늘 보여야 한다.

**도구줄·시점줄도 44px 로 올렸다 (09-22 · 관리자가 정했다).**
`.hud button{min-height:44px;min-width:44px;padding:0 12px}` **한 규칙만** 고쳤다. 자바스크립트는 한 줄도 안 바꿨다.

| 375px | 전 | 후 |
|---|---|---|
| 도구줄 선택·궤도·이동·전체 | 50×**28** | **50×44** |
| 도구줄 줌 | **38×28** | **44×44** |
| 시점줄 등각·정면·측면 | 50×**28** | **50×44** |
| 시점줄 위·뒤 | **38×28** | **44×44** |
| 도구줄 · 시점줄 폭 | 262 · 250 | **268 · 262** (칸 347 안 — 둘 다 **한 줄** 그대로) |
| `.hud` 높이 · 3D 가린 비율 | 145px · 30.9% | **176px · 37.6%** (숨김바까지 뜨면 199→230px · 42.5→49.1%) |

- **`.hud` 는 `position:absolute` 라 캔버스는 안 줄어든다**(375×468 그대로). 가리는 자리만 31 → 38% 로 는다.
- 1280px 에서는 도구줄이 원래 숨겨져 있고(§4.7) 시점줄은 `.modeseg` 가 줄을 늘려 이미 44 였다 —
  **위·뒤만 38 → 44** 로 바뀌었다. 줄 폭 250 → 262.
- 자리·글자·모양·흐려지는 동작은 그대로다(4.2초 뒤 흐림 0, 숨김바는 1 그대로).
  손가락으로 「이동」 을 탭하면 `pan:true` — 전·후가 같다.

## 4.898 지운 부속은 목록 아래 「비활성 부속」 으로 내려 모은다 (09-22 사장님 말씀)

사장님 말씀(보드선택 「상판」 줄을 짚으시고): 「**이미삭제된 부속은 비활성 부속으로 목록아래에 따로 만들어 주세요**」
바로 앞 04:49 에 「이 엑스가 활성 비활성 표기되는 이유는?」 하고 물으신 그 자리다.

**⚠ 설정 때문에 없는 것은 안 내린다.** 도어 0짝 · 이동선반 0단 · 전면밴드 0 은 **지운 것이 아니다.**
내리면 도어 짝수를 0 으로 두는 순간 도어 두께 칸이 저 아래로 튀어 사장님이 못 찾으신다.
`지우기단추갱신()` 의 `없음`(= 도면에 없다)으로는 이 둘을 못 가른다.
**무엇이 「지운 것」 인지는 `되살릴것()` 이 이미 정확히 센다**(`손질.지움` 의 붙박이 + `지난부속` 에 있고 `만든부속` 에 없는 것). 그걸 그대로 쓴다.

**줄은 옮기기만 한다 — 다시 그리지 않는다.** 그래서 두께 고르개·×·결 단추·덮기/끼우기가 **그대로 먹는다**(듣는 곳이 그 노드에 붙어 있다).
- 여덟 붙박이 줄에 **`data-home`(제자리 번호)·`data-part`(부속 이름)** 를 붙였다. 되살리면 **번호대로 제자리**에 돌아간다.
- 되돌릴 때는 `보드.insertBefore(줄, 뒤에 올 산 줄 || #backInWrap)` 로 넣는다. **자리가 맞으면 건드리지 않는다** — 쓸데없이 DOM 을 흔들면 열린 고르개가 닫힌다.
- **지운 만든 부속은 줄 자체가 없어진다**(`만든부속` 에서 빠지므로). `지난부속` 에서 **같은 모양으로 다시 그린다**(`#deadCustom`).
  그 줄의 두께 고르개는 `지난부속` 의 `T` 를 고친다 — 되살릴 때 그 두께로 선다.
- 지운 것이 하나도 없으면 무리째 **안 보인다**(`#deadWrap.hidden`).
- **되살리는 길은 예전 그대로 「부속 만들기」 칸의 드롭다운이다**(§4.89). **비활성 줄에 「되살리기」 단추를 붙이지 마라** — 사장님이 청하지 않으셨다.

**잰 값 (375px · 진짜 손가락 탭)**

| | 보드 줄 | 비활성 줄 | 보드 칸 높이 | 비활성 높이 |
|---|---|---|---|---|
| 지운 것 없음 | 여덟 다 | – (무리 숨김) | 733 | **0** |
| **도어 0짝 · 이동선반 0단 · 전면밴드 0** | **여덟 다 그대로** | **–** | – | **0** |
| 상판 하나 지움 | 일곱 | 상판 | 782 | 86 |
| 셋 지움(상판·고정선반·뒷판) | 다섯 | **상판 · 고정선반 · 뒷판**(원래 차례) | 790 | 194 |
| 밴드(만든 부속)까지 | 다섯 | 상판 · 고정선반 · 뒷판 · **밴드** | 804 | 248 |

1280px 에서도 같다(보드 칸 1157 → 1206 → 1214 → 1228 · 비활성 0 → 126 → 316 → 410).

**옮긴 줄에서 다 먹는다 (하나하나 진짜로 눌러 봤다)**
- 결 단추 → `결설정 {"상판":"세로"}` · 두께 고르개 → `Ttop 18 → 25` · 덮기/끼우기 → `backMode cover → insert`
- × 는 **흐리고(opacity .3)** 다시 눌러도 안 지워지며 「**이미 빠져 있습니다 — 「부속 만들기」 칸을 누르면 되살립니다**」 라고 알린다.
- 크기: × 보이는 32×32 · **닿는 44×44** · 두께 70×40 · 결 단추 71×31 · **닿는 44px**.
- 고르개에서 되살리면 **제자리로 돌아온다**(밴드 → 만든 부속 줄, 상판 → 보드 첫 줄). 다 되살리면 무리가 숨는다.
- DXF 네 값·저장 SVG 그대로. 오류 0.

**⚠ 여기서 드러난 예전 탈 — 안 고쳤다(사장님 확인 전).**
**지운 붙박이의 두께를 바꾸면 되살아난다.** `손질.지움` 열쇠에 자리(x,y,z)가 들어 있어서다(§4.8 끝·§4.86 과 같은 탈).
잰 값 — 뒷판을 지우면 `뒷판@2,397,82`, 두께를 9T 로 바꾸면 자리가 `뒷판@2,391,82` 로 달라져 **열쇠가 안 맞아 도로 선다.**
상판(`Ttop` 18→25)·뒷판(덮기→끼우기)도 같다. **고치기 전 판(fcf12cc)에서도 똑같다 — 이번에 생긴 것이 아니다.**
고치려면 문짝·밴드처럼 붙박이에도 **굳은 열쇠**를 붙여야 한다. 관리자에게 숫자로 알렸다.

## 4.89 부속 목록 · 지웠던 부속 되살리기 (09-17 사장님 말씀)

사장님 말씀: 「부속을 삭제하는 버튼을 모든 부속에 적용. 되살리려면 부속만들기 칸을 클릭하면
지금까지 설정한 모든 부속이 드롭다운으로 나오고, **이미 도면에 반영된 부속은 목록에 안 보이게**.」

- **× 는 위쪽 「보드선택」 두께 줄마다 있다.** 09-17 에 목록판(`#partList`)을 따로 만들었다가
  사장님이 **「아니 이게 아니라 이 삭제 버튼을 저 위에 부속두께 설정 하는곳에 놔주라고」** 하셔서 걷어냈다.
  **판을 새로 만들지 마라.** 만든 부속 줄(`#customList`)의 × 와 **같은 모양·같은 자리**(`.two > select + button.x`)다.
  보이는 것은 32×32px 그대로 두고 **`::after` 로 닿는 자리만 44×44px** 로 넓혔다. 두께 칸과는 **10px** 띄웠다.
  도면에 없는 부속의 × 는 **흐리게(opacity .3)** 두고, 눌리면 되살리는 길을 알린다(`지우기단추갱신()` 이 `update()` 끝에서 맞춘다).
- **되살리기는 「부속 만들기」 칸에 붙는 드롭다운**(`#partNames`)이다. 09-17 에 두 번 되돌린 자리다 —
  ① 칸 아래에 딸림말 붙은 카드 판을 띄웠다가 사장님이 **「이렇게가 아니고 … 칸 안에 드롭다운 목록으로」** 하셨고,
  ② 그래서 `datalist` 로 바꿨는데 **브라우저가 그리는 목록이라 줄 높이를 못 정한다**(집 규칙 44px). 그래서 직접 그린다.
  **판을 따로 띄우지 마라. 딸림말도 붙이지 마라 — 이름만이다.**
  `.addrow` 가 `position:relative` 고 `.namedrop` 이 **칸 바로 아래 2px, 칸과 같은 폭**으로 붙는다. 줄 **44px**.
  **도면에 선 것은 안 나온다.** 적는 대로 걸러진다. 되살릴 것이 없으면 아예 안 열린다.
  **고르면 칸에 이름만 들어간다. 되살리는 것은 「추가」 다 (09-22 사장님 말씀).**
  사장님 말씀: 「드롭다운 선택 후 **추가 버튼을 누르면** 다시 활성화 부속으로 추가되어야 하는데
  지금은 **드롭 선택과 동시에 활성화** 되어버림」. 09-17 에 관리자 뜻대로 「고르면 바로」 로 두었던 것을
  **사장님 말씀대로 되돌렸다**(§0 — 사장님 글자가 이긴다). **다시 「고르면 바로」 로 바꾸지 마라.**
  「추가」 는 이름이 `되살릴것()` 에 있으면 **붙박이든 만든 부속이든 되살린다** —
  그 검사를 「이미 있는 이름입니다」 막이보다 **먼저** 둔다(안 그러면 상판·측판 같은 붙박이가 막힌다).
  목록에 없는 새 이름은 예전처럼 적고 「추가」 다. 두 길이 한 단추로 모인다.

  **잰 값 (375px · 진짜 손가락)** — 상판·밴드를 지우고 고르개를 열면 목록 「밴드 · 상판」, 줄 **267×44**.
  「상판」 을 고르면 칸에 **「상판」 만 들어가고 아무것도 안 되살아난다**(목록은 닫힌다).
  「추가」 를 눌러야 보드 첫 줄로 돌아온다(토스트 「상판 되살렸습니다」).
  밴드도 같고 **두께·단이 그대로다**(`{이름:'밴드',T:25,단:['상','하']}`).
  새 이름 「덧판」 추가 그대로, 이미 있는 이름 「측판」 은 그대로 막힌다(「이미 있는 이름입니다 — 측판」).

**지우는 길이 둘이다 — 갈래마다 맞는 것을 쓴다. 섞지 마라.**

| 갈래 | 지울 때 | 되살릴 때 |
|---|---|---|
| 만든 부속 | `만든부속` 에서 빼고 **`지난부속`** 에 통째로 적어 둔다 | `지난부속` 에서 꺼내 되돌린다 — **두께·단까지 그대로** |

**만든 부속의 × 도 `부속지우기()` 를 거쳐야 한다.** 예전처럼 `만든부속.splice()` 만 하면 `지난부속` 에 안 적혀 **고르개에서 못 되살린다** — 한 번 그랬다.
| 붙박이 부속 | 그 이름의 조각 `자리` 를 `손질.지움` 에 넣는다(3D 차림표의 삭제와 같은 길) | `손질.지움` 에서 그 이름을 다 뺀다 |

`지난부속` 은 `localStorage` 의 `…지난부속` 에 담긴다. 「추가」 로 같은 이름을 다시 적어도 지난 값을 살린다.

**구조 부속을 지워도 프로그램이 버틴다 (다 재 봤다).**
측판·하판·상판·뒷판·문짝·전면밴드·고정선반을 하나씩 지워 3D·도면·DXF 를 다 뽑아 봤다 — **오류 0**.
지운 뒤 DXF: 측판 65,723 · 하판 68,606 · 상판 68,511 · 뒷판 55,213 · 문짝 54,485 · 전면밴드 70,484 · 고정선반 60,352.
**되살리면 매번 바이트까지 돌아온다**(그때 기준 72,301 · 지금 기준은 §1).
**일곱을 다 지워도 버틴다** — 3D 조각 0, 도면 1쪽, 부품표 0줄, DXF 9,554B(표제란만), 오류 0.
다 되살리면 부속 11 · 부품표 7줄 · **그때 기준 72,301B**(지금 기준은 §1).

**잰 값 (375px)** — 붙박이 7줄 다 44×44, **위아래 틈 10px**(잘못 눌리지 않는다). 고정선반 × → 도면·부품표에서 빠지고 고르개에 「고정선반 · 지운 것 되돌리기」.
밴드를 25T·[상,하] 로 만들고 × → 고르개에 「밴드 · 두께 25mm」 → 되살리니 **`{T:25, 단:['상','하']}` 그대로**.
도면에 서 있는 것은 고르개에 **안 나온다**(다 되살리면 「되살릴 부속이 없습니다」).

**안 뺏은 것** — 3D 차림표의 숨기기·삭제·복제, 관계제어판의 「손질한 부속」 되돌리기 그대로다(재 봤다).
§6 대로 **붙박이 삭제를 막지 않는다** — 측판·상판도 지워진다.

## 4.88 밴드를 3D 에서 끌어 올리고 내린다 — **덮기일 때만** (09-17 사장님 말씀)

사장님 말씀: 「덮기일 때 뒷판의 라인에서 자유롭게 이동. 단 하판·상판을 벗어날 수 없고,
하판·고정선반·상판의 끝점들에서 적정위치 걸림. 3D 에서 선택 후 마우스로 이동.」

- **뒷판 라인(y)은 안 바뀐다.** 위아래(z)로만 간다. **끼우기는 말씀에 없어 그대로 둔다 — 안 끌린다.**
- 칸은 `밴드바닥 = bz + Tbot` ~ `밴드천장 = (H − Ttop) − 밴드키` 다.
  기본값에서 **98 ~ 1684** — 마침 「하」 자리와 「상」 자리가 그 두 끝이다.
- **걸림자리**(밴드 **아랫끝** 기준) = 하판 윗면 · 고정선반마다 **윗면**(얹기)과 **아랫면 − 밴드키**(달기) · 상판 밑면.
  기본값(고정 3단)에서 **98 · 407.5 · 523.5 · 833 · 949 · 1258.5 · 1374.5 · 1684** 여덟 자리다.
- **걸리는 자는 화면 14px 어치다**(고정 mm 가 아니다). 기본 배율에서 약 77mm —
  14mm 로 박았더니 4px 밖에 안 되어 손에 걸리는 느낌이 전혀 안 났다. 그 사이는 자유롭게 선다.
- 옮긴 자리는 **`c.옮김[단]`** 에 담긴다(`담기()` 로 저장). 단(하·상)마다 따로다.
  **새로 열어도 그 자리에 선다**(잰 값: 949 로 끌고 새로고침 → 949, 저장값 `{이름:'밴드',T:18,옮김:{하:949}}`).
- **상·중·하 단추는 「처음 서는 자리」 다.** 단추를 **끄면 그 단의 `옮김` 을 턴다** — 다시 켜면 제자리에 선다
  (잰 값: 949 → 하 끔 → 다시 켬 → **98**, 옮김 `{}`). 관리자가 정한 것이다. **이 한 줄을 지우지 마라.**
- **옛 설정은 그대로 열린다** — `{이름:'밴드',T:18}` 처럼 `단`·`옮김` 이 없는 값을 넣고 열면 **하:98** 하나만 서고
  「하」 단추만 켜져 있다. 기본값을 바꾸지 마라.

**걸리는 자리는 어느 면인가 (재서 정했다)**

| 무엇 | 밴드 아랫끝이 붙는 곳 | 뜻 |
|---|---|---|
| 하판 | **윗면** (`bz+Tbot`) | 하판 위에 얹힌다 |
| 고정선반 | **윗면** (`선반z+t`) | 선반 위에 얹힌다 |
| 고정선반 | **아랫면 − 밴드키** | 밴드 **윗끝**이 선반 아랫면에 닿는다(선반 밑에 달린다) |
| 상판 | **밑면 − 밴드키** | 밴드 윗끝이 상판 밑면에 닿는다 |

**걸림자리에서는 겹침이 0 이다**(여덟 자리 다 쟀다). 그런데 **걸림자리 사이에 세우면 고정선반을 뚫는다** —
선반 높이(465.5 · 891 · 1316.5)에 세우면 **764 × 15 × 18mm**. 15자리 가운데 3자리에서 났다.
**밀지 않았다. 사장님 확인 전이다.**

**밴드를 옮기면 내보내는 파일이 달라진다** — 도면에 그 자리가 그려지기 때문이다.
잰 값: 밴드 z98 **74,290B** → z949 **74,391B**(101B 늘고 2,390줄 다름). **부품표는 그대로다**(764×98×15).
밴드가 아예 없는 기본 상태는 **그때 기준 72,301 · 109,127 그대로**였다(지금 기준은 §1).

**끄는 동안에는 조각만 움직이고, 손을 뗄 때 모델에 담는다.** `update()` 를 끌 때마다 부르면
`rebuild3D` 가 `selPid = null` 로 되돌리고 조각을 새로 세워 끌던 것을 놓친다. 이 차례를 바꾸지 마라.
손을 뗀 뒤에는 같은 단의 밴드를 다시 골라 준다.

**고른 그 조각만 겨눈다**(`광선.intersectObject(조각)`). 앞에 선반이나 상판이 가려도 잡힌다 —
안 그러면 밴드가 선반 뒤로 들어간 순간 **다시 잡을 길이 없어진다**(한 번 그렇게 막혔다).

**안 뺏은 것 — 이 셋을 지켜라.**
- 아무것도 안 골랐으면 왼쪽 끌기는 **예전 그대로 화면 이동**이다(재 봤다: 기준 0,0,0 → −152,−420,399).
- **톡 친 것**(5px·손가락 10px 안)은 옮기지 않고 예전처럼 **고르기 · 두 번째 톡은 손질 차림표**로 간다.
- **길게 누르기(지시 손잡이)는 그대로다** — 손가락은 10px 넘게 끌어야 비로소 옮기기가 되고,
  가만히 누르면 `.vg-sheet` 가 뜬다(`?viggle=1` 로 재 봤다. 밴드는 안 움직였다).
- 폰에서는 **「선택」 연장일 때만** 손가락으로 끈다(선택은 지금도 화면을 안 움직이므로 뺏는 것이 없다).
  잰 값 — 톡 쳐서 고르고 손가락으로 끌어 98 → 523.5(고정선반 윗면에 걸림), 도면·부품표가 따라온다.

**손질 열쇠 — 옮겨도 따라온다 (09-17 고침).**
`자리(p)` 는 `이름@x,y,z` 라 **밴드를 옮기면 열쇠가 바뀌어 숨김·지움이 풀렸다.** 그래서 옮길 수 있는 밴드에는
**`p.단열쇠 = '밴드@단:하'`** 처럼 단(段)으로 붙잡는 열쇠를 따로 붙이고, `열쇠들(p)` 로 찾는다.

```
열쇠들(p) = p.단열쇠 ? [p.단열쇠, 자리(p)] : [자리(p)]      // 앞엣것이 새로 적을 열쇠
```
- 숨김·지움·복제를 찾을 때 **둘 다 본다.** 그래서 **옛 설정에 담긴 자리 열쇠가 그대로 먹는다** —
  재 봤다: `밴드@18,379,98`(숨김) · `측판@0,0,0`(지움) · `고정선반@18,0,506`(숨김·복제 +2 → 부품표 3→5) 다 그대로 먹는다.
- **「중」 에는 안 붙인다** — 선반마다 여럿이라 단 하나로는 서로 못 가린다(덮기에선 안 서고 움직이지도 않는다).
- 잰 값: 949 로 끌고 숨긴 뒤 전면밴드를 80→120 으로 바꿔도 **숨김 그대로**. 지움도 그대로.

**붙박이를 지워도 3D 가 안 터진다 (진짜 탭에서 다 재 봤다).**
측판·상판·하판·뒷판·고정선반·문짝·전면밴드를 하나씩, 그리고 **일곱을 몽땅** 지운 채로 덮기·끼우기 둘 다 —
webgl 살아 있고, 삼각형이 그려지고(11조각 132 → 9조각 108 …), 「전체(Shift+Z)」 가 기준 0,0,0 · r 3920 으로 돌아온다.
**오류 0.** 다 지우면 조각 0 · 삼각 0 이지만 축선은 그대로 그려지고 탈이 안 난다.

## 4.87 보호대 — 전면밴드 칸을 채우는 부속 (09-17 사장님 말씀)

사장님 말씀 그대로: 「측판의 바닥에서 하판까지 위치에 채워지는 부속. 사이즈는 **측판 바닥에서 하판 바닥까지**로 조절됩니다.」
「부속 만들기」 로 **「보호대」** 를 넣으면 선다. 밴드와 같은 `자리있는부속` 이다.

| | 보호대 | 밴드 「하」 |
|---|---|---|
| z | **0 ~ `bz`** (측판 바닥 ~ 하판 **밑면**) | `bz+Tbot` ~ `2(bz+Tbot)` |
| 재단 | **764 × 80 × 18** | 764 × 98 × 18 |

**둘의 차이는 하판 두께 하나다** — 보호대는 하판 **바닥**까지라 `Tbot` 을 안 먹는다. 이 한 가지를 흐리지 마라.
폭은 밴드와 같은 `W − 2·Tside`. **전면밴드가 0 이면 설 자리가 없어 0개다**(`키 > 0` 일 때만 선다).

**앞뒤(y) 자리는 사장님이 안 정하셨다** — 「밴드와 비슷한 개념」 이라고만 하셨다.
우선 **밴드와 같은 규칙**으로 두었다: 덮기 `CD − 두께`, 끼우기 `홈뒤`. **관리자에게 숫자로 알렸다.**

- **어느 설정에서도 부딪히지 않는다** — 덮기·끼우기 × 12T·18T × 전면밴드 0·80·120 × 인·아웃도어 × W1200 H2400 다 0mm.
  전면밴드는 `y 30~48` 로 앞을 막고 보호대는 뒤에 서므로 앞뒤로 만나지 않는다.
- **다만 뒤가 완전히 막히지는 않는다** — 보호대 위끝 80, 뒷판 아랫끝이 **덮기 82 · 끼우기 92.5** 라
  **덮기 2mm · 끼우기 12.5mm** 가 뚫린 채 남는다. 사장님이 정하신 키(`bz`)를 지키면 피할 수 없다. **안 밀었다.**
- 넘침(측판 뒤끝을 넘어 나온 길이) — **덮기 0 · 끼우기 18T 9mm · 12T 3mm.** 밴드와 같은 값이다.

**15mm 경고는 밴드 규칙 그대로 걸었다** (사장님이 따로 말씀 안 하셔서 관리자 지시대로 같은 규칙을 썼다).
`밴드살피기` 가 이제 `자리있는부속` 을 돌며 `m.넘침들[이름]` 을 본다. 한 번에 하나만 알린다.
잰 값 — 보호대 18T·끼우기: 붉게 9표본 → 「보호대가 측판보다 튀어나옵니다 … 9mm … 15mm 로 맞췄습니다」 → 15 로 조정 → 카메라 복귀.
보호대 18T·덮기, 보호대 15T·끼우기는 **안 뜬다**. **밴드 25T 는 예전과 똑같이 뜬다**(16mm).

**보호대에는 「상·중·하」 단추가 없다** — 그건 밴드만이다(`c.이름 !== '밴드'` 로 갈린다).

## 4.86 문짝 열쇠는 「짝 번호」 다 — 인도어·아웃도어를 넘나든다 (09-22 사장님 말씀)

사장님 말씀: 「**아웃도어 선택시 도어 자체가 사라지는현상**이 있어 수정해줘」.

**모델은 멀쩡했다.** 인·아웃 둘 다 문짝 2장이 제 크기로 선다 —
아웃 `x 2·402 · y −18 · z 85 · 396×18×1713` / 인 `x 20·401.5 · y 0 · z 101 · 377.5×18×1678`.
**탈은 `손질` 열쇠였다.** `자리(p) = 이름@x,y,z` 라 인도어 자리(`문짝@20,0,101`)와
아웃도어 자리(`문짝@2,-18,85`)가 서로 다른 열쇠다. 그래서 **한쪽에서 숨기거나 지운 문짝이
다른 쪽에서 되살아나고, 되돌아오면 또 사라졌다.** `state` 는 저장하지 않아 **열면 늘 아웃도어**다 —
그래서 아웃도어에서 지운 문짝은 **열 때마다 없는 채로** 떴다.

고친 길은 밴드와 같다 — 짝 번호로 붙잡는 열쇠를 따로 붙인다.

```
parts[…].단열쇠 = `문짝@짝:${k}`      // buildModel 의 문짝 for 문 안
```

**옛 문짝 자리 열쇠는 한 번만 턴다.** 그대로 두면 열쇠가 먹어 **문짝이 계속 없는 채로** 열린다 —
사장님이 보시는 탈이 안 고쳐진다. 그래서 담긴 값을 읽을 때 `/^문짝@-?\d/` 인 열쇠(숨김·지움·복제)를 버린다.
**문짝 것만 버린다** — `측판@0,0,0` · `고정선반@18,0,506` · `밴드@18,379,98` 은 그대로 먹는다(재 봤다).
지워 둔 문짝은 **되살아난다.** 다시 지우시면 이제는 두 모드에 다 남는다.

**잰 값 (375px · 진짜 손가락 탭)**

| | 고치기 전 | 고친 뒤 |
|---|---|---|
| 아웃에서 × → 인도어 → 아웃도어 | 0 → **2** → 0 (오락가락) | 0 → **0** → 0 |
| 아웃에서 숨김 → 인도어 → 아웃도어 | 숨김 2 → **0** → 2 | 숨김 **2 → 2 → 2** |
| 담긴 옛 열쇠 `문짝@2,-18,85` 지움 | 아웃 0 · 인 2 | **아웃 2 · 인 2** (한 번 털림) |
| 옛 `측판@0,0,0` · `고정선반@18,0,506` · `밴드@18,379,98` | 그대로 먹음 | **그대로 먹음** |

3D 에서 문짝을 톡 치면 `문짝@짝:0` 으로 골라지고, 두 번째 톡의 차림표는 126×44 그대로다.
숨긴 부속 목록은 「문짝 1713×396 · **자리 짝:0**」 으로 적힌다(전에는 `자리 2,-18,85`).
네 기준값 그대로(그때는 72,301 · 109,127 · 71,222 · 108,233 · §1 참고), 저장 SVG 도 네 상태 다 같음.

## 4.85 이름 두 가지 — 헷갈리지 마라

- **전면밴드** = 예전 「걸레받이」 다(09-17 사장님이 이름을 바꾸라 하셨다). 붙박이 부속이다.
  **저장 열쇠는 옛 이름 그대로 두었다** — `규칙.걸레앞` · `규칙.아웃아래걸레` · `state.plinth` · `state.Tplinth`.
  이름을 바꾸면 사장님 브라우저에 저장된 값이 날아간다. **건드리지 마라.**
  손질 열쇠(`자리`)는 부속 이름을 쓰므로 바뀌었다 — 옛 `걸레받이@…` 는 이제 안 먹는다(§4.8 끝 참고).
- **밴드** = 「부속 만들기」 로 세우는 뒷판 쪽 덧대는 것이다(§4.8). **전면밴드와 다른 것이다.**
- **보호대** = 전면밴드 칸(z 0~`bz`)을 채우는 부속이다(§4.87). **밴드와도 전면밴드와도 다른 것이다.** 이름 셋이 비슷하니 조심해라.

## 4.9 덮기 뒷판은 네 쪽에서 2mm 들어간다 (09-16 사장님 말씀)

**덮기(cover) 만이다. 끼우기는 `뒷판키움` 규칙 그대로 — 건드리지 마라.**
`규칙.뒷판물림 = 2` 한 값이 정한다. **관계제어판에는 손잡이를 안 만들었다**(관계표 배열에 안 넣었다).

```
x  물림 ~ W − 물림        (좌우 측판 끝에서 2mm 씩 들어감)
z  bz + 물림 ~ H − 물림   (하판 밑면에서 2mm 올라가고, 상판 끝에서 2mm 내려옴)
```

| 설정 | x | z | 재단 |
|---|---|---|---|
| W800 H1800 걸80 하18 (기본값) | 2 ~ 798 | **82 ~ 1798** | **1716 × 796** |
| 전면밴드 0 | 2 ~ 798 | 2 ~ 1798 | 1796 × 796 |
| 걸120 하25 | 2 ~ 798 | 122 ~ 1798 | 1676 × 796 |
| W1200 H2400 걸80 | 2 ~ 1198 | 82 ~ 2398 | 2316 × 1196 |

상판 결합이 「덮기(overlay)」 여도 위끝은 `H − 2` 그대로다(상판 윗면이 곧 H 다).
**「하판 바닥」 을 하판 밑면(`bz`)으로 읽었다** — 그래서 전면밴드 80 이면 아랫끝이 82 이고
**전면밴드 뒤가 트인다**(전에는 뒷판이 바닥 0 까지 내려와 가렸다). 관리자에게 숫자로 알렸다.

## 4.10 바닥 격자는 꺼 두었다

사장님 말씀(09-16 「3D 도면의 바닥 경계그물이 안 보이게」)으로 `makeGrid` 의 **`바닥격자 = false`** 다.
코드는 그대로 두고 보이기만 껐다 — 도로 켜려면 그 한 줄을 true 로 두면 된다.
축선(빨강·초록·파랑)은 그대로다. 사장님이 격자만 짚으셨다.

## 4.96 결 방향은 부속마다 켜는 것이다 — 안 켜면 아무 데도 안 적는다 (09-22 사장님 말씀)

사장님 말씀: 「**결방향 표기 가로 세로 세팅 하게 해서 세팅없을시 (결없음, 기본세팅) 결방향 표기 안되게** 해줘」

**담는 곳** — `결설정 = { 부속이름: '가로'|'세로' }`, `localStorage` 의 `…결`. 없으면 「결없음」 이고 **그것이 기본값**이다.
읽는 길은 `결표기(이름)` 하나뿐이다.

**단추** — 두께 줄(× 가 있는 그 줄)에 `가로` `세로` 두 개다. 하나 켜면 다른 쪽이 꺼지고,
**켠 것을 다시 누르면 둘 다 꺼진다 = 결없음.** `.seg.mini` 라 닿는 자리는 `::after` 로 **44px** 다.
듣는 곳은 `document` 하나(위임)라 만든 부속 줄이 새로 그려져도 다시 걸 일이 없다.
`결단추갱신()` 은 `지우기단추갱신()` 바로 뒤, `update()` 끝에서 부른다.

**넓은 화면은 두 줄로 쌓는다.** 설정 칸이 284px 로 못 박혀 있어(§4.6) 한 줄에 넣으면
「가로/세로」 글자가 칸을 넘친다(쟀다: 고정선반 단추 **28px**). `@media (min-width:1101px)` 에서
`grid-template-areas:"이름 두께" "단추 단추"` 로 쌓아 **119×31** 이 된다. 폰은 한 줄 그대로다(단추 68~86×31).

**결이 나가는 곳은 다섯이다. 결없음이면 다섯 다 안 나간다.**

| 어디 | 줄 |
|---|---|
| 도면·DXF 부품표 표의 결 칸 | `871` |
| 부속 쪽 도면의 **결 화살표 + 「결 ○○」** | `1137`~`1141` (화살표까지 통째로 안 그린다) |
| 부속 쪽 정보 칸의 「결」 줄 | `1156` |
| 화면 부품표의 결 칸 | `1648` |
| 부품표 PDF | `2044` |

다섯 다 `r.grain` 을 읽고, `r.grain` 은 `bomRows`(`958`·`962`)와 `부속행들`(`1084`·`1087`)에서
**`결표기(이름)` 로 덮어쓴다**. `p.cut.grain`(buildModel 이 넣는 본디 결)은 **그대로 두었다** —
buildModel 은 한 글자도 안 바뀌었다(md5 네 상태 다 같음).

**잰 값**

| | 결없음(기본) | 측판만 세로 | 예전 값 여덟 다 켬 |
|---|---|---|---|
| 덮기 DXF | **72,203** | 72,217 (+14) | **72,301** (옛 기준과 바이트까지 같음) |
| 부품표 결 칸 | 일곱 줄 다 빈칸 | 측판만 「세로」 | 일곱 줄 다 참 |
| 부속 쪽 「결 ○○」 · 화살표선 | **0 · 0** | 1 · 5 | 7 · 35 |
| 부속 쪽 정보 칸 「결」 줄 | **0** | 1 | 7 |

- DXF 는 **딱 7줄**만 달라진다(`\U+C138\U+B85C` → 빈 줄). 줄 수는 15,795 로 같다.
- 108가지 치수 겹침 쓸기 다시 돌림 — 숫자 1,692 · 지시선 48 · **겹침 0** (표에서 글자 7개가 빠져도 그대로다).
- 새로 열어도 담긴다(`{"측판":"세로",…}`). 오류 0.

**안 고친 것 — 사장님·관리자 확인 전이다.**
1. **머리글은 그대로다** — 화면·PDF·DXF 다 「결 길이 · 결 폭 · 결」 이다(`442`·`1647`·`2033`·`871`).
   결이 하나도 없어도 「결 길이/결 폭」 이라 적혀 있다. **관리자가 「머리글은 네 맘대로 바꾸지 마라」 하셨다.**
   내 생각 — 머리글은 **그대로 두는 것이 맞다.** 결을 안 적어도 그 치수가 나뭇결 방향 치수라는 뜻은 그대로고,
   머리글을 바꾸면 CAD 로 받는 표의 칸 이름이 달라진다. 바꾼다면 「길이 · 폭」 이겠는데 뜻이 흐려진다.
2. **「재단 치수 mm · 가로 = 결 길이 · 세로 = 결 폭」 안내**(`1114`)도 그대로다. 부속 쪽마다 나온다.
   결없음에서는 가리킬 것이 없는 안내가 된다. **관리자에게 알렸다.**
3. **관계제어판의 결 줄은 09-22 에 고쳤다 (관리자가 정했다).** 박아 둔 `값:() => '세로 3 · 가로 4'` 가
   결없음에서 거짓말이었다. 이제 `bomRows(m)` 의 결을 **세어 적는다**. 말도 지금 코드가 하는 일로 고쳤다 —
   「결은 부속마다 보드선택에서 켠다. 안 켜면 「결없음」 이라 어디에도 결을 안 적는다.」, `어디:'보드선택'`.
   잰 값 — 결없음 **「없음 7」** · 측판만 세로 **「세로 1 · 가로 0 · 없음 6」** · 여덟 다 켬 **「세로 3 · 가로 4 · 없음 0」**(옛 박힌 값과 같다).
   **그 줄 하나만 고쳤다.** DXF 네 숫자 그대로(제어판은 도면에 안 들어간다).
4. **`가로/세로` 를 바꿔도 `결 길이(L)`·`결 폭(W)` 은 안 바뀐다.** 사장님 말씀이 「표기」 라 표기만 바꿨다.
   측판을 「가로」 로 켜도 결 길이 1800 · 결 폭 397.3 그대로다. 치수를 맞바꿔야 하는지 **안 여쭈었다.**

## 4.95 판(모달)이 열려 있어도 짚어서 지시가 되게 (09-17 사장님 말씀)

사장님 말씀: 「관계제어판 팝업창 나가기 버튼 없어요. 그리고 **관계제어판 상태에서 짚어서메세지 보낸창에 입력을 못합니다**」

**까닭** — 모달 `<dialog>` 는 브라우저가 **판 바깥을 통째로 inert** 로 만든다.
손잡이(`shared/viggle.js`)는 지시창 `.vg-sheet` 를 **`document.body` 에 붙인다** — 즉 판 바깥이라 글이 안 들어간다.
**판 하나만의 일이 아니다.** `#rulesDlg`·`#bomDlg`·`#dlg`·`#warnDlg`·`#zoomDlg` 가 다 같다.

**고친 길** — 손잡이는 다른 저장소 것이라 **못 고친다.** 이쪽에서 `MutationObserver` 로 `.vg-sheet` 가
`body` 에 붙는 것을 지켜보다가 **열린 판 안으로 그 노드를 옮긴다.** 같은 노드를 옮기는 것이라 손잡이가 나중에 지우는 것도 그대로 된다.
판이 닫히면 `close` 에서 **다시 `body` 로 돌려놓는다. 판을 닫아 버리지 않는다.**
`?viggle=1` 묶음 **밖**에 두었다 — 손잡이가 어떤 길로 들어오든 걸리게.

**잰 값 (375px · `?viggle=1` · 진짜 탭에서 길게 눌러 글자를 쳐 봤다)**

| 판 | 고치기 전 | 고친 뒤 |
|---|---|---|
| 판 없이 | 「시험 글자」 ✔ | 「시험 글자」 ✔ |
| 관계제어판 · 부품표 · DXF · 경고 · 도면크게 | **「」 — 한 글자도 안 들어감** | **「시험 글자」 ✔ · 보내기 167×46 눌림** |

판 열린 채 지시창을 띄워도 **판은 그대로 열려 있고**, 판을 닫으면 지시창이 `body` 로 돌아가며 **친 글자도 남는다**.

**나가기(×) 는 관계제어판에만 붙였다.** 375px 에서 다섯 판을 다 재 보니 그것만 손이 안 닿았다 —
`.dlg` 가 굴러가는 칸인데 **속 3,887px / 보이는 593px** 이라 맨 아래 「닫기」 가 **y3,927** 에 있었다.
`.dlghead` 를 `position:sticky; top:0` 로 두고 **44×44px** × 를 얹었다(`#rX`). 1,500px 굴려도 y108~152 에 그대로 있다.
`.dlg.rdlg{padding-top:0}` 이다 — 위에 틈을 두면 굴러 올라온 글이 그 틈으로 비친다.
아래 「닫기」(`#rClose`)는 그대로 두었다. 나머지 넷은 나가기가 첫 화면에 보인다(부품표 닫기 y663 · DXF y551 · 경고 확인 y403 · 도면크게 닫기 y35, 화면 780).

## 5. 3D 가 안 뜨면 도면·부품표까지 통째로 안 나온다

`three.js` 를 바깥(cdnjs)에서 받는다. 못 받으면 `init3D()` 가 터지고 그 뒤의 `update()` 가 아예 안 돌아
**도면과 부품표까지 빈 화면**이 된다. 3D 만 안 보이는 것이 아니다.
**아직 안 고쳤다 — 사장님께 여쭈어 둔 상태다. 멋대로 고치지 마라.**

## 6. 안 건드리는 것

- **길게 누르기(0.5초)** — 지시 손잡이(`viggle.js`)가 쓴다. 3D 화면의 **10px 자**(`drag.held`)도 그것 때문에 있다.
- 톡 쳐서 부속 고르기 · 고른 것 한 번 더 톡 쳐서 손질 차림표 (폰에는 우클릭이 없다).
- 붙박이 부속의 **삭제를 막지 않는다.** 숨기기는 **3D 와 도면 둘 다**에서 빼고 **부품표에는 남긴다.**

## 7. 이 방에서 못 하는 것

- **바깥 인터넷이 막혀 있다** (cdnjs · jsdelivr · github.io 다 연결 거부). 그래서 테스트는 로컬 복사본으로 한다.
- **아이폰 사파리(웹킷)가 없다.** 프린터도 없다. **진짜 기기 확인은 사장님 몫이다** — 목록에 들고 있지 않는다.

**3D 는 복사본에서 돈다 — 「못 본다」 고 적지 마라.** `three.min.js` 를 옆에 두고 주소만 바꾸면
렌더러가 살아 있고 조각·카메라·재질을 다 잴 수 있다(밴드 붉은 깜박임 8번, 카메라 3,920→680,
등각 0.75·1.15 로 도는 것까지 이 방에서 쟀다). 한 번 「3D 가 안 떠서 못 봤다」 고 잘못 답한 적이 있다.

**배포된 판은 못 연다.** github.io 가 막혀 있어 「판번호 올린 뒤 그림을 찍어라」 를 그대로 못 한다.
**푸시한 그 소스로 만든 복사본**을 찍고, **배포 확인은 관리자 몫이라고 답에 적는다.** 찍은 척하지 마라.

## 7.5 재는 법 — 여기서 발목 잡힌 것들

**복사본 만들고 띄우기** (스크래치패드의 `mk.py`·`up.sh`)
`mk.py` 가 cdnjs 주소를 옆에 둔 `three.min.js`·`viggle.js` 로 바꾸고 jszip 을 지우고
**`window.__probe`** 를 심는다(`set`·`model`·`draw`·`dxf`·`svgf`·`grp`·`sel`·`cam`·`aim`·`rend`·`T`·`손질`·`parts`·`st`).
`up.sh` 로 http-server 를 띄운다 — **가만두면 죽으므로** 시험 전에 다시 띄운다.
견줄 판은 `git show <커밋>:index.html` 로 따로 뽑아 다른 포트에 띄운다.

**여기서 헛짚었던 것 — 같은 데를 또 밟지 마라**
- **3D 모드에서는 도면이 `display:none`** 이라 치수 숫자가 **0개**로 나온다. 도면을 재려면 **2D 로 바꾼 뒤** 재라(§4.58).
- **폰에서 캔버스가 화면 밖에 있다.** 375px 에서 `#c3d` 가 `y 1612` 에 있어 그 자리를 톡 쳐도 아무 일이 없었다
  (로그가 텅 비어 「손가락이 안 먹는다」 고 헛짚었다). **`scrollIntoView` 로 끌어온 뒤 자리를 다시 재라.**
  한 번 톡 치면 설정 칸이 접혀(§4.7) 자리가 또 바뀐다 — **탭마다 다시 재라.**
- **두께 고르개는 `change` 가 아니라 `input`** 을 듣는다.
- **`state` 는 저장하지 않는다.** `localStorage` 에 담는 것은 넷뿐이다 —
  `…규칙` · `…부속`(만든 부속) · `…손질` · `…지난부속` · `…결`. 그래서 **열면 늘 기본값**이다(아웃도어·덮기·800×400×1800).
  새 탭에서 `localStorage` 를 심을 때 열쇠 이름은 `cabinet-studio.…` 그대로 쓴다.
- **`__probe.parts()` 에 값을 넣어도 안 담긴다** — `담기()` 를 안 부르기 때문이다. `addInitScript` 로 심어라.
- **카메라를 손으로 돌려 놓으면(`cam.theta = …`) 화면 카메라는 안 따라온다** — 다 닿으면 다시 그리지 않는다.
  잴 때는 **`가만`(theta·phi·r·tgt 가 목표에 닿을 때까지 기다리기)** 을 거친 뒤 재라.
- **CSS 이김을 조심해라.** `#pageBox>.page:not(.보는쪽)` 을 클래스로 썼더니 뒤에 오는
  `.svgwrap .page.p1{display:flex}` 가 이겨 **쪽 두 장이 나란히 떴다.** 아이디로 써야 이긴다.
- **DXF·저장 SVG 는 바이트로만 믿어라.** `anchor`(네 기준값) · `svgsame`(저장 SVG 조각 견주기) ·
  `dxfsame`(줄 단위 md5) 셋을 돌린다. 「다른 줄 7」 처럼 **어느 줄이 달라졌는지**까지 적어라.
- **글자배가 달라지는 고침이면 108가지 쓸기를 다시 돌려라**(§4.55). 도면 칸 크기를 건드리면 글자배가 달라진다.

## 8. 일 끝내는 법

1. 잰 값(숫자)을 남긴다 → 2. `main` 에 커밋·푸시 → 3. `box:cabinet-studio` 에 답을 붙인다.

```bash
cd /home/user/vivivic-cloud/viggle
VG_WHO=캐비닛 python3 tools/답하기.py "box:cabinet-studio" "…"
python3 tools/지시읽기.py box:cabinet-studio      # 밀린 지시 보기
```
커밋 글은 짧게. **커밋 제목에 안 한 것을 적지 않는다.**
**커밋 글에 잰 전후 숫자를 적는다** (관리자가 두 번 짚었다). 제목 한 줄 + 숫자 두세 줄이면 된다.
**모델 이름은 어디에도 적지 않는다** — 커밋·PR·주석·코드 어디에도.

**답에 꼭 들어갈 것** — ① 무엇을 고쳤는지 한 줄 ② 전후 숫자 표 ③ **안 달라진 것**(네 기준값·저장 SVG·오류 0)
④ **내가 판단으로 더 한 것**과 되돌렸을 때의 숫자 ⑤ **못 한 것·확인 못 한 것**.
